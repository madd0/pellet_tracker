set_level:
  fields:
    level:
      required: true
      selector:
        number:
          min: 0
          max: 100
          unit_of_measurement: "%"
          mode: box
    entry_id:
      required: true
      selector:
        config_entry:
          integration: pellet_tracker
    calibrate:
      selector:
        boolean:
